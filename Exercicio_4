#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    float a, b, h;
    cout << "Informe os catetos: ";
    cin >> a;
    cin >> b;

    h = pow(a,2) + pow(b,2);

    cout << "Valor da hipotenusa: "<<h<<endl;
    cout << "Valor raiz da hipotenusa: "<<sqrt(h);
    return 0;
}